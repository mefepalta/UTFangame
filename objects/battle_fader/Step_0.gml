// Siyah ortu ~0.5 sn'de aciliyor, renkli parlama ise arkasindan
// ~1.2 sn boyunca sonuyor; gecis boylece sert bir kesme degil,
// onceki sahnenin isiginin sonmesi gibi duruluyor.
if (alpha > 0) alpha = max(alpha - 0.034, 0);
if (bloom > 0) bloom = max(bloom - 0.011, 0);
